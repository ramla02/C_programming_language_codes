#include <stdio.h>
/**
 * The main function is the start of the program.
 * The following codes is an example to show a few escape sequence characters.
 * When the below code is compiled and executed, it produces tabbed string
 * literals.
 * Return: 0 for the successful execution.
 */
int main(void)
{
printf("Hello\tWorld\n\n");
return (0);
}
